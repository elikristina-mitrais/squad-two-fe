## Setup in Local


``` bash
# clone from repository
$ git clone https://github.com/elikristina-mitrais/squad-two-fe.git

# go to the working directory
$ cd squad-two-fe

# create .env.local file and add REACT_APP_SERVER_URL value

# install dependencies
$ npm install

# serve with reload at localhost:3000
$ npm start
```
